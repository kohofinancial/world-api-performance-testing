name: Non-Go linting

on:
  workflow_dispatch:
  push:
    branches:
      - main
  pull_request:

jobs:
  lint:
    name: Non-Go lint code base
    uses: kohofinancial/actions/.github/workflows/linter.yml@main
